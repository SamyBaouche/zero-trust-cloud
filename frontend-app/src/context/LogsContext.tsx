/**
 * LogsContext (placeholder).
 * <p>
 * The current version of the frontend loads logs directly inside pages (Dashboard/Logs page).
 * If, later, we want to share log state across multiple screens (caching, polling, etc.),
 * we can implement a React context here.
 */

export {}



